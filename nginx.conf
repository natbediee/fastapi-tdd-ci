events {}

http {
  server {
    listen 80;

    location /api/ {
      proxy_pass http://app:8000/;
      proxy_set_header Host $host;
    }

    location /grafana/ {
      proxy_pass http://grafana:3000/;
      proxy_set_header Host $host;
    }
  }
}